# Live Security Test — 2026-05-28
Target: http://168.119.15.122
Tester: Automated (devin-delegate)

## Findings

| # | Category | Severity | Finding | Evidence |
|---|----------|----------|---------|---------|
| 1 | Security Headers | MEDIUM | Missing critical security headers | No X-Frame-Options, X-Content-Type-Options, CSP, HSTS, X-XSS-Protection, or Referrer-Policy headers present |
| 2 | CORS Policy | LOW | CORS credentials enabled without explicit origin | `Access-Control-Allow-Credentials: true` returned without `Access-Control-Allow-Origin` specification |
| 3 | Rate Limiting | MEDIUM | No rate limiting detected | 20 consecutive requests to `/v1/cannes/tokenomics/status` all returned 200 OK |
| 4 | Information Disclosure | LOW | package.json exposed at root | `http://168.119.15.122/package.json` returns full package metadata with dependencies |
| 5 | HTTP Methods | INFO | Dangerous methods not allowed | DELETE, TRACE, PUT return empty responses (properly blocked) |
| 6 | Path Traversal | INFO | Path traversal attempts handled | `../../../etc/passwd` and URL-encoded variants return React app HTML (no file system access) |
| 7 | SQL Injection | INFO | Injection attempts handled | SQL injection probes on query params return empty or error responses |
| 8 | Error Handling | INFO | Clean error responses | 404 and validation errors return structured JSON without stack traces |

## Security Headers Audit

| Header | Present | Value |
|--------|---------|-------|
| X-Frame-Options | ❌ Missing | - |
| X-Content-Type-Options | ❌ Missing | - |
| Content-Security-Policy | ❌ Missing | - |
| Strict-Transport-Security | ❌ Missing | - |
| X-XSS-Protection | ❌ Missing | - |
| Referrer-Policy | ❌ Missing | - |
| Access-Control-Allow-Credentials | ✅ Present | true |
| Cache-Control | ✅ Present | no-cache |

## Recommendations

### HIGH PRIORITY
1. **Add security headers** - Implement at minimum:
   - `X-Frame-Options: DENY` or `SAMEORIGIN`
   - `X-Content-Type-Options: nosniff`
   - `Content-Security-Policy` with appropriate directives
   - `Strict-Transport-Security: max-age=31536000; includeSubDomains` (HTTPS only)

2. **Implement rate limiting** - Add rate limiting to API endpoints to prevent abuse and DoS:
   - Start with 100-200 requests per minute per IP
   - Consider stricter limits for sensitive endpoints

### MEDIUM PRIORITY
3. **Restrict package.json exposure** - Move package.json out of web root or block access via web server configuration
4. **Configure CORS properly** - Either specify explicit allowed origins or remove `Access-Control-Allow-Credentials` if not needed

### LOW PRIORITY
5. **Consider adding additional headers**:
   - `Referrer-Policy: strict-origin-when-cross-origin`
   - `Permissions-Policy` header to restrict browser features

## Positive Findings
- Error responses are clean JSON without stack traces
- Path traversal attacks are properly handled
- SQL injection attempts appear to be mitigated
- Dangerous HTTP methods (DELETE, TRACE, PUT) are blocked
- Sensitive files (.env, .git/config) are not exposed
- API documentation files (docs.json, openapi.json, swagger.json) are not exposed